[build]
  command = "npm run build"
  # Do NOT set publish directory - @netlify/plugin-nextjs handles it automatically

# Use Netlify Next.js Runtime plugin for optimal performance
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

